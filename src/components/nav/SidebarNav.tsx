export default function SidebarNav() {
  // Placeholder sidebar for future per-page sub-navigation
  // Keeping structure minimal for now
  return (
    <nav aria-label='Sidebar' className='sidebar-nav' style={{ color: '#9fb3ff' }}>
      {/* Intentionally empty - ready for future content */}
    </nav>
  );
}
